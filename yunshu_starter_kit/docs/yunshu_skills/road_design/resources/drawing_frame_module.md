# 图框模块化规范 (Drawing Frame Module Standard)

## 1. 模块概述 (Overview)
本模块定义了标准图框 (`tk.dwg`) 的使用规范、变量定义及自动化出图流程。图框作为外部参照 (Xref) 统一管理，确保所有图纸的图签风格一致，且易于批量修改。

## 2. 核心文件 (Core File)
*   **文件名**: `tk.dwg` (建议名称)
*   **存储位置**: (待定义，通常在项目资源库中)
*   **使用方式**: 作为 **外部参照 (External Reference)** 插入到设计图纸中。

## 3. 动态变量定义 (Variables)
图框中预设了关键文本变量，用于在新项目启动时动态替换：

| 变量代码 | 含义 | 对应内容 (Example) | 必填性 |
| :--- | :--- | :--- | :--- |
| **aa** | **公司名称** (Company Name) | (待定) | ✅ 必填 |
| **bb** | **工程名称** (Project Name) | (待定) | ✅ 必填 |

## 4. 工作流集成 (Workflow Integration)
在生成或处理 DXF/DWG 图纸时：
1.  **加载底图**: 读取包含 `tk.dwg` 外部参照的标准图纸。
2.  **文本替换**: 将 `aa`, `bb` 等变量替换为实际项目信息。
3.  **保存输出**: 生成最终交付的图纸文件。
